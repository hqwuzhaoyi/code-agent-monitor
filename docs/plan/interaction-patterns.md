# CAM 交互模式研究报告

## 研究目标

分析用户通过 OpenClaw Agent (clawdbot) 与 CAM 交互的最佳模式，设计自然对话交互体验。

---

## 1. 现有交互设计分析

### 1.1 CAM Skill 当前设计

**优点：**
- 完整的自然语言意图映射表
- 上下文管理机制（current_agent_id, current_project）
- 模糊输入处理策略
- 详细的示例对话

**问题：**
- 过于依赖工具调用细节暴露给用户
- 通知转发是"原样转发"而非智能汇总
- 缺少对话状态机设计
- 用户回复处理过于机械（1/2/3 映射）

### 1.2 cam-notify Skill 当前设计

**优点：**
- 清晰的 urgency 分级
- 简洁的转发消息格式

**问题：**
- 仅做转发，不做智能处理
- 用户回复映射过于简单
- 缺少上下文感知

### 1.3 coding-agent Skill 参考

**值得借鉴：**
- PTY 模式强调（交互式终端需求）
- 进度更新策略（只在关键节点通知）
- Auto-Notify 完成机制
- 清晰的参数说明

---

## 2. 用户典型交互流程

### 2.1 启动 Agent

```
用户意图表达方式：
├── "帮我在 xxx 项目跑个 Claude"
├── "启动一个 agent"
├── "开个新的"
└── "在 /path/to/project 启动"

理想响应：
├── 明确时：直接启动，返回简洁确认
└── 模糊时：询问项目路径（提供最近项目列表）
```

### 2.2 查看状态

```
用户意图表达方式：
├── "看看在干嘛"
├── "现在跑着什么"
├── "进度怎样"
├── "怎么了"
└── "卡住了吗"

理想响应：
├── 无 agent：告知无运行任务
├── 单 agent：直接显示状态摘要
└── 多 agent：列表展示，标注等待状态
```

### 2.3 响应权限请求

```
用户意图表达方式：
├── "y" / "yes" / "好" / "可以" / "允许"
├── "n" / "no" / "不" / "拒绝"
├── "继续" / "go"
└── 数字选择 "1" / "2" / "3"

理想响应：
├── 有明确上下文：直接发送到对应 agent
├── 无上下文：询问"你是要回复刚才的权限请求吗？"
└── 多 agent 等待：列出选项让用户选择
```

### 2.4 处理错误

```
通知格式：
"❌ cam-xxx 出错了：[错误摘要]"

用户可能回复：
├── "看看详情" → 显示完整错误日志
├── "重试" → 重新发送上一条指令
├── "停掉" → 停止 agent
└── "怎么办" → AI 分析错误并建议

理想响应：
├── 主动分析错误类型
├── 提供可能的解决方案
└── 询问用户下一步
```

### 2.5 查看结果

```
用户意图表达方式：
├── "看看结果"
├── "做了什么"
├── "改了哪些文件"
└── "完成了吗"

理想响应：
├── 汇总最近操作（不是原样输出日志）
├── 列出修改的文件
├── 标注任务完成状态
```

---

## 3. "自然对话"交互模式设计

### 3.1 核心原则

1. **意图优先**：理解用户想做什么，而非要求用户说出工具名
2. **上下文感知**：记住当前 agent、最近操作、等待状态
3. **智能汇总**：AI 处理信息后再呈现，不原样转发
4. **最小打扰**：只在需要用户决策时通知

### 3.2 对话状态机

```
┌─────────────┐
│   IDLE      │ ← 无运行 agent
└──────┬──────┘
       │ 启动 agent
       ▼
┌─────────────┐
│  RUNNING    │ ← agent 执行中
└──────┬──────┘
       │ 检测到等待
       ▼
┌─────────────┐
│  WAITING    │ ← 等待用户输入
└──────┬──────┘
       │ 用户回复
       ▼
┌─────────────┐
│  RUNNING    │
└──────┬──────┘
       │ 任务完成/退出
       ▼
┌─────────────┐
│   IDLE      │
└─────────────┘
```

### 3.3 用户回复 "y" / "继续" 的处理逻辑

```python
def handle_short_reply(reply: str, context: Context):
    """处理简短回复"""

    # 1. 检查是否有等待中的 agent
    waiting_agents = get_waiting_agents()

    if len(waiting_agents) == 0:
        # 无等待 agent，可能是误触或想恢复会话
        return "目前没有等待中的任务。要查看最近的会话吗？"

    if len(waiting_agents) == 1:
        # 单 agent 等待，直接发送
        agent = waiting_agents[0]
        send_to_agent(agent.id, normalize_reply(reply))
        return f"已发送到 {agent.project}，继续执行中..."

    # 多 agent 等待，需要用户选择
    return format_agent_choices(waiting_agents, reply)

def normalize_reply(reply: str) -> str:
    """标准化用户回复"""
    positive = ["y", "yes", "好", "可以", "允许", "继续", "go", "是"]
    negative = ["n", "no", "不", "拒绝", "否"]

    if reply.lower() in positive:
        return "y"
    if reply.lower() in negative:
        return "n"
    return reply  # 原样发送
```

### 3.4 "看看进度" 的智能响应

```python
def handle_progress_query(context: Context):
    """处理进度查询"""

    agents = get_running_agents()

    if not agents:
        return "目前没有运行中的任务。"

    summaries = []
    for agent in agents:
        status = get_agent_status(agent.id)

        # 智能汇总，不是原样输出
        summary = {
            "project": agent.project_name,
            "status": "等待确认" if status.waiting_for_input else "执行中",
            "recent_action": summarize_recent_tools(status.recent_tools),
            "context_usage": f"{status.context_percent}%",
        }

        if status.waiting_for_input:
            summary["waiting_for"] = status.wait_context

        summaries.append(summary)

    return format_progress_summary(summaries)
```

**输出示例：**

```
📊 当前任务状态

1. myapp 项目
   状态：等待确认
   等待：创建 src/utils.rs？[Y/n]
   最近：编辑了 main.rs, Cargo.toml

2. backend 项目
   状态：执行中
   最近：运行测试
   上下文：45%
```

### 3.5 通知智能汇总

**原始通知（不应直接转发）：**
```
🔐 [CAM] cam-1770350313 请求权限

工具: Bash
命令: npm install && npm run build && npm test

[CAM_META] urgency=HIGH event_type=permission_request agent_id=cam-1770350313
```

**智能汇总后：**
```
🔐 myapp 项目需要确认

要执行：安装依赖并运行测试
命令：npm install && npm run build && npm test

回复 y 允许，n 拒绝
```

**汇总规则：**
1. 用项目名替代 agent_id
2. 用自然语言描述命令意图
3. 移除元数据
4. 简化回复选项

---

## 4. 交互优化建议

### 4.1 减少用户认知负担

| 当前 | 优化后 |
|------|--------|
| "cam-1770350313 请求权限" | "myapp 项目需要确认" |
| "回复: 1=允许 2=记住 3=拒绝" | "回复 y 允许，n 拒绝" |
| "Agent 在等待输入" | "等你确认：[具体内容]" |
| 显示完整终端输出 | 智能摘要 + "查看详情" 选项 |

### 4.2 上下文记忆

```yaml
session_context:
  current_agent: cam-xxx        # 最近交互的 agent
  current_project: myapp        # 当前项目
  last_notification:            # 最近通知
    agent_id: cam-xxx
    type: permission_request
    timestamp: 2024-01-15T10:30:00
  pending_confirmations: []     # 待确认列表
```

### 4.3 快捷回复

| 用户输入 | 解释为 |
|----------|--------|
| y / 好 / 可以 | 允许当前请求 |
| n / 不 / 拒绝 | 拒绝当前请求 |
| 继续 / go | 发送 "继续" 到当前 agent |
| 停 / 取消 | 停止当前 agent |
| 1 / 2 / 3 | 选择对应选项 |
| 看看 / 进度 | 显示状态摘要 |

### 4.4 主动行为

1. **任务完成时**：主动汇总做了什么，而非只说"完成了"
2. **长时间无响应**：提醒用户有等待中的确认
3. **错误发生时**：分析错误并建议解决方案
4. **多 agent 场景**：主动标注哪个在等待

---

## 5. 实现建议

### 5.1 Skill 层改进

**cam-notify skill 改进：**
```markdown
## 通知处理流程

1. 解析通知元数据
2. 查询 agent 对应的项目名
3. 分析命令/操作意图
4. 生成自然语言描述
5. 根据 urgency 决定是否转发
6. 转发时使用汇总格式
```

**code-agent-monitor skill 改进：**
```markdown
## 上下文管理

维护以下状态：
- current_agent_id: 最近交互的 agent
- pending_confirmations: 待确认请求队列
- last_status_check: 上次状态检查时间

## 快捷回复处理

当用户输入为单字符或简短词时：
1. 检查是否有 pending_confirmations
2. 有则发送到对应 agent
3. 无则询问用户意图
```

### 5.2 通知路由优化

```
HIGH urgency:
├── 权限请求 → 智能汇总后转发
├── 错误 → 分析后转发 + 建议
└── 等待输入 → 汇总上下文后转发

MEDIUM urgency:
├── 空闲等待 → 汇总最近操作后转发
└── Agent 退出 → 汇总完成内容后转发

LOW urgency:
├── 启动 → 仅记录，不转发
└── 工具调用 → 仅记录，不转发
```

### 5.3 消息格式标准化

```
# 权限请求
🔐 {project_name} 需要确认
{operation_description}
回复 y 允许，n 拒绝

# 等待输入
⏸️ {project_name} 等你回复
{context_summary}

# 任务完成
✅ {project_name} 完成了
{changes_summary}
要查看详情吗？

# 错误
❌ {project_name} 出错了
{error_summary}
{suggestion}
```

---

## 6. 测试场景

### 6.1 快捷回复测试

```bash
# 场景：有一个 agent 在等待确认
用户: y
预期: 直接发送到等待中的 agent，返回"已发送，继续执行中..."

# 场景：有多个 agent 在等待
用户: y
预期: 列出等待中的 agent，询问"回复哪个？"

# 场景：无 agent 在等待
用户: y
预期: "目前没有等待中的任务。要查看最近的会话吗？"
```

### 6.2 进度查询测试

```bash
# 场景：单 agent 运行中
用户: 看看进度
预期: 显示该 agent 的状态摘要

# 场景：多 agent 运行
用户: 现在跑着什么
预期: 列表显示所有 agent 状态

# 场景：无 agent
用户: 在干嘛
预期: "目前没有运行中的任务。"
```

### 6.3 通知汇总测试

```bash
# 输入：原始权限请求通知
# 预期：汇总为自然语言描述，用项目名替代 agent_id

# 输入：原始错误通知
# 预期：分析错误类型，提供建议
```

---

## 7. 总结

### 核心改进点

1. **从工具导向转为意图导向**：用户说"继续"而非"调用 send_input"
2. **智能汇总替代原样转发**：AI 处理信息后再呈现
3. **上下文感知**：记住当前 agent，支持快捷回复
4. **减少认知负担**：用项目名替代 agent_id，简化选项

### 实现优先级

1. **P0**：快捷回复处理（y/n/继续）
2. **P0**：通知智能汇总
3. **P1**：上下文状态管理
4. **P1**：进度查询智能响应
5. **P2**：错误分析与建议
6. **P2**：多 agent 场景优化
